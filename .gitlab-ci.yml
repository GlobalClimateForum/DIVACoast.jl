docs:
  image: julia:1.8.5
  stage: deploy
  script:
    - julia -e "using Pkg; Pkg.add('Documenter')"
    - julia ./docs/make.jl
    - mkdir .public
    - cp -r docs/build/* .public
    - mv .public public
  artifacts:
      paths:
        - public
  only:
    - development